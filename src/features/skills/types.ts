export type SkillFilters = {
  query: string;
  category: string;
};

export type SkillListFilters = {
  category: string;
};

export type SkillCategoryCount = {
  category: string;
  count: number;
};
